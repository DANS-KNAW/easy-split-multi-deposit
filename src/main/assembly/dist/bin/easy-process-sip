#!/usr/bin/env bash

BINPATH=`readlink -f $0`
APPHOME=`dirname \`dirname $BINPATH \``

java -Dlogback.configurationFile=$APPHOME/cfg/logback.xml \
     -Dconfig.file=$APPHOME/cfg/application.conf \
     -Dapp.home=$APPHOME \
     -jar $APPHOME/bin/easy-process-sip.jar $@
